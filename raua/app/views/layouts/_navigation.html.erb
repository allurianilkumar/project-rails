<%= react_component('Navigation', 
                    {:presenter => @presenter.to_json, :imgSrc => image_path("logo.png")}, 
                    {:prerender => true}) %>
<%= react_component('Sidebar', 
                    {:presenter => @presenter.to_json, :imgSrc => image_path("logo.png")}, 
                    {:prerender => true}) %>
<%= react_component('Registration', 
                    {:presenter => @presenter.to_json, :imgSrc => image_path("logo.png")}, 
                    {:prerender => true}) %>